# ─── Default Profile Activation ──────────────────────────────────────────────
# This ensures 'dev' profile is always active when no other profile is specified.
# On production/render, override with: -Dspring.profiles.active=prod
spring.profiles.active=dev
